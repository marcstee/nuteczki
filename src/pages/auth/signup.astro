---
import Layout from "@/layouts/Layout.astro";
import SignUpForm from "@/components/auth/SignUpForm";

const error = Astro.url.searchParams.get("error");
---

<Layout title="Zarejestruj się">
  <div class="flex min-h-screen items-center justify-center p-4">
    <div class="border-border bg-card text-foreground w-full max-w-sm rounded-2xl border p-8">
      <h1 class="text-primary mb-6 text-center text-2xl font-bold">Zarejestruj się</h1>
      <SignUpForm serverError={error} client:load />
      <p class="text-muted-foreground mt-4 text-center text-sm">
        Masz już konto? <a href="/auth/signin" class="text-secondary hover:underline">Zaloguj się</a>
      </p>
    </div>
  </div>
</Layout>
